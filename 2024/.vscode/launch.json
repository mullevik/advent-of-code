{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Test day 10",
            "request": "launch",
            "command": "npm run test day_10",
            "cwd": "${workspaceFolder}"
        }
    ]
}